// FirebaseConfig was removed — credentials are managed by native config files.
// Drop google-services.json / GoogleService-Info.plist directly into the
// android/app/src/<flavor>/ and ios/config/<flavor>/ directories.
