name: flutter_forge
description: Interactive CLI that scaffolds production-grade Flutter projects.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  dart_console: ^1.2.0
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^6.0.0

executables:
  flutter_forge: flutter_forge
  flutter_forge_generate: generate
  flutter_forge_color: color
